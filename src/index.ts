import firebaseDataProvider, {
  CREATE_WITHOUT_AUTOMATIC_ID_KEY,
  fb,
} from "./firebaseDataProvider";

export {
  firebaseDataProvider as FirebaseDataProvider,
  fb as firebaseClientInstance,
  CREATE_WITHOUT_AUTOMATIC_ID_KEY,
};
